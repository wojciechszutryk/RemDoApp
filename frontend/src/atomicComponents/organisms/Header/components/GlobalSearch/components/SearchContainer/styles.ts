import { styled } from "@mui/material";

export const StyledSearchContainerWrapper = styled("div")(() => ({
  width: "100%",
}));

export const SearchResultsWrapper = styled("div")({
  padding: "10px 5%",
});

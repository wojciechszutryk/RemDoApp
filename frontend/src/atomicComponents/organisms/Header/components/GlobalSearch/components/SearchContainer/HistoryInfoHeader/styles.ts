import { styled } from "@mui/material";

export const StyledWrapper = styled("div")({
  paddingLeft: "25px",
  display: "flex",
  height: "47px",
  alignContent: "center",
  alignItems: "center",
  justifyContent: "space-between",
});

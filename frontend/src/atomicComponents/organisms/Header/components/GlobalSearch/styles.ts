import { styled } from "@mui/material";

export const GlobalSearchWrapper = styled("div")({
  display: "block",
  width: "100%",
  height: "100%",

  "@media (max-width: 1024px)": {
    marginLeft: "auto",
  },
});

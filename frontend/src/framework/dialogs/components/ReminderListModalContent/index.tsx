import { Button } from "@mui/material";
import { memo } from "react";

const ReminderListModalContent = (): JSX.Element => {
  return (
    <>
      <Button></Button>
    </>
  );
};

export default memo(ReminderListModalContent);

import { memo } from 'react';

interface Props {};

const TaskModal = (props: Props): JSX.Element => {
    return (
        <div>
            
        </div>
    );
};

export default memo(TaskModal);